#!/usr/bin/env bash
# Tiny shim so `tracer` can live on PATH.
# Resolves real path so symlinking into ~/.local/bin works.
SELF="$(readlink -f "$0" 2>/dev/null || python3 -c "import os,sys; print(os.path.realpath(sys.argv[1]))" "$0")"
DIR="$(dirname "$SELF")"
exec python3 "$DIR/analyzer.py" "$@"
